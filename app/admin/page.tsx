import Link from "next/link";
import { Dashboard } from "@/components/admin/dashboard";
import { LogoutButton } from "@/components/admin/logout-button";
import { collectionLabels } from "@/lib/cms";
export default function AdminPage() { return <main className="min-h-screen bg-background"><header className="border-b border-line"><div className="mx-auto flex max-w-[90rem] items-center justify-between px-5 py-5"><Link href="/admin" className="font-display text-xl">AYZENT / ADMIN</Link><LogoutButton /></div></header><div className="mx-auto grid max-w-[90rem] gap-10 px-5 py-10 lg:grid-cols-[15rem_1fr]"><nav className="grid grid-cols-2 gap-2 text-sm sm:grid-cols-3 lg:block lg:space-y-1">{Object.entries(collectionLabels).map(([key, label]) => <Link key={key} href={`/admin/${key}`} className="block border border-line px-3 py-2 hover:text-gold lg:border-0">{label}</Link>)}</nav><section><p className="text-xs font-medium uppercase tracking-[.18em] text-gold">Overview</p><h1 className="mt-3 font-display text-4xl">Good to see you.</h1><div className="mt-10"><Dashboard /></div></section></div></main>; }
