import type { MetadataRoute } from "next";
import { getPosts, getProducts, getProjects } from "@/lib/content";
export default async function sitemap(): Promise<MetadataRoute.Sitemap> { const base = (process.env.NEXT_PUBLIC_SITE_URL || "http://localhost:3000").replace(/\/$/, ""); const pages = ["", "/about", "/services", "/projects", "/products", "/blog", "/careers", "/contact"]; const [projects, products, posts] = await Promise.all([getProjects(), getProducts(), getPosts()]); return [...pages.map((path) => ({ url: `${base}${path}`, lastModified: new Date(), changeFrequency: "monthly" as const, priority: path === "" ? 1 : .7 })), ...projects.map((item) => ({ url: `${base}/projects/${item.slug}`, lastModified: new Date(), changeFrequency: "monthly" as const, priority: .8 })), ...products.map((item) => ({ url: `${base}/products/${item.slug}`, lastModified: item.updatedAt || new Date(), changeFrequency: "weekly" as const, priority: .8 })), ...posts.map((item) => ({ url: `${base}/blog/${item.slug}`, lastModified: new Date(), changeFrequency: "monthly" as const, priority: .7 }))]; }
