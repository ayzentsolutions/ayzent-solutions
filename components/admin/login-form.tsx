"use client";
import { useRouter } from "next/navigation";
import { useState, type FormEvent } from "react";
import { Button } from "@/components/ui/button";
export function LoginForm() { const router = useRouter(); const [message, setMessage] = useState(""); const [pending, setPending] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); const data = Object.fromEntries(new FormData(event.currentTarget)); const response = await fetch("/api/admin/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }); const result = await response.json(); if (response.ok) router.replace("/admin"); else setMessage(result.message); setPending(false); } return <form onSubmit={submit} className="grid gap-5"><label className="grid gap-2 text-sm">Email<input required name="email" type="email" className="border border-line bg-transparent px-3 py-3" /></label><label className="grid gap-2 text-sm">Password<input required minLength={10} name="password" type="password" className="border border-line bg-transparent px-3 py-3" /></label><Button disabled={pending} type="submit">{pending ? "Signing in…" : "Sign in"}</Button>{message && <p role="status" className="text-sm text-red-700">{message}</p>}</form>; }
