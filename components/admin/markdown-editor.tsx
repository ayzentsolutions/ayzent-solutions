"use client";
import { useRef } from "react";
import { Markdown } from "@/components/content/markdown";
export function MarkdownEditor({ id, value, required, onChange }: { id: string; value: string; required?: boolean; onChange: (value: string) => void }) { const ref = useRef<HTMLTextAreaElement>(null); function wrap(before: string, after = before, placeholder = "text") { const element = ref.current; if (!element) return; const start = element.selectionStart; const end = element.selectionEnd; const selected = value.slice(start, end) || placeholder; const next = `${value.slice(0, start)}${before}${selected}${after}${value.slice(end)}`; onChange(next); requestAnimationFrame(() => { element.focus(); element.setSelectionRange(start + before.length, start + before.length + selected.length); }); } return <div className="grid gap-3"><div className="flex flex-wrap gap-2"><button type="button" onClick={() => wrap("**")} className="border border-line px-2 py-1 text-xs">Bold</button><button type="button" onClick={() => wrap("*")} className="border border-line px-2 py-1 text-xs">Italic</button><button type="button" onClick={() => wrap("## ", "", "Heading")} className="border border-line px-2 py-1 text-xs">Heading</button><button type="button" onClick={() => wrap("[", "](https://)", "Link text")} className="border border-line px-2 py-1 text-xs">Link</button><button type="button" onClick={() => wrap("- ", "", "List item")} className="border border-line px-2 py-1 text-xs">List</button></div><textarea ref={ref} id={id} required={required} value={value} onChange={(event) => onChange(event.target.value)} rows={12} className="w-full resize-y border border-line bg-transparent px-3 py-3 font-mono text-sm" /><div className="border border-line p-4"><p className="mb-4 text-xs font-medium uppercase tracking-[.14em] text-gold">Live preview</p><Markdown content={value || "Preview your formatted article here."} /></div></div>; }
