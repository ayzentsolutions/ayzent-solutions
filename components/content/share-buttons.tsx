"use client";
import { useState } from "react";
export function ShareButtons({ title }: { title: string }) { const [message, setMessage] = useState(""); const url = typeof window === "undefined" ? "" : window.location.href; async function copy() { try { await navigator.clipboard.writeText(window.location.href); setMessage("Link copied"); } catch { setMessage("Unable to copy the link"); } } async function share() { if (!navigator.share) return copy(); try { await navigator.share({ title, url: window.location.href }); setMessage(""); } catch { /* User closed native share. */ } } return <div className="mt-12 flex items-center gap-5 border-y border-line py-5 text-sm"><button type="button" onClick={copy} className="hover:text-gold">Copy link</button><button type="button" onClick={share} className="hover:text-gold">Share this {title.includes(" ") ? "page" : "project"}</button>{message && <span role="status" className="text-muted">{message}</span>}</div>; }
