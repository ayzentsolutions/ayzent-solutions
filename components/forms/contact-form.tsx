"use client";
import { useState, type FormEvent } from "react";
import { Button } from "@/components/ui/button";
const services = ["Website Development", "Digital Marketing", "SEO", "Website & Software Maintenance", "UI/UX Design", "Deployment & Cloud Services"];
const budgets = ["Under $5,000", "$5,000 – $15,000", "$15,000+"];
export function ContactForm() { const [message, setMessage] = useState(""); const [pending, setPending] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); setMessage(""); const form = new FormData(event.currentTarget); const response = await fetch("/api/inquiries", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(form)) }); const data = await response.json(); setMessage(data.message); if (response.ok) event.currentTarget.reset(); setPending(false); } return <form className="grid gap-5" onSubmit={submit}><div className="grid gap-5 sm:grid-cols-2"><Field label="Name" name="name" required /><Field label="Email" name="email" type="email" required /></div><div className="grid gap-5 sm:grid-cols-2"><Field label="Phone" name="phone" type="tel" /><Field label="Company name" name="company" /></div><Select label="Required service" name="service" values={services} /><Select label="Estimated budget" name="budget" values={budgets} /><label className="grid gap-2 text-sm">Project description<textarea name="description" required rows={5} className="resize-y border border-line bg-transparent px-3 py-3 text-foreground" placeholder="What are you hoping to achieve?" /></label><Button type="submit" disabled={pending} className="mt-2 w-fit">{pending ? "Sending…" : "Send Inquiry"}</Button>{message && <p role="status" className="text-sm text-muted">{message}</p>}<p className="text-xs text-muted">We will only use your details to respond to your inquiry.</p></form> }
function Field({ label, name, type = "text", required = false }: { label: string; name: string; type?: string; required?: boolean }) { return <label className="grid gap-2 text-sm">{label}<input name={name} required={required} type={type} className="border border-line bg-transparent px-3 py-3 text-foreground" /></label> }
function Select({ label, name, values }: { label: string; name: string; values: string[] }) { return <label className="grid gap-2 text-sm">{label}<select name={name} required defaultValue="" className="border border-line bg-transparent px-3 py-3 text-foreground"><option value="" disabled>Choose an option</option>{values.map((value) => <option key={value}>{value}</option>)}</select></label> }
